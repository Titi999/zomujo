export enum Gender {
  Male = 'male',
  Female = 'female',
  Other = 'other',
}

export enum Status {
  Verified = 'verified',
  Unverified = 'unverified',
}

export enum Role {
  Admin = 'admin',
  SuperAdmin = 'super_admin',
  Doctor = 'doctor',
  Patient = 'patient',
}
